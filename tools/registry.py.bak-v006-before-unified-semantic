from tools.browser import (
    open_page,
    get_state,
    fill,
    click,
    click_text,
    click_role,
)


TOOLS = [
    {
        "type": "function",
        "function": {
            "name": "browser_open_page",
            "description": (
                "Открывает URL в постоянной браузерной сессии Chromium. "
                "Возвращает состояние страницы, интерактивные элементы "
                "с element_id и screenshot."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "url": {
                        "type": "string",
                        "description": "Полный URL страницы"
                    }
                },
                "required": ["url"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_get_state",
            "description": (
                "Получает текущее состояние браузера и список "
                "интерактивных элементов с element_id."
            ),
            "parameters": {
                "type": "object",
                "properties": {}
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_fill",
            "description": (
                "Вводит обычный несекретный текст в поле по element_id. "
                "Не использовать для паролей, токенов и других секретов."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "element_id": {
                        "type": "string"
                    },
                    "text": {
                        "type": "string"
                    }
                },
                "required": [
                    "element_id",
                    "text"
                ]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_click_role",
            "description": (
                "Находит и нажимает элемент по accessibility role "
                "и доступному имени. Используй в первую очередь для "
                "вкладок, кнопок, ссылок и других элементов, когда "
                "их роль известна. Например: role=tab, name=Agent."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "role": {
                        "type": "string",
                        "description": (
                            "Accessibility role: например tab, "
                            "button, link, checkbox"
                        )
                    },
                    "name": {
                        "type": "string",
                        "description": "Доступное имя элемента"
                    },
                    "exact": {
                        "type": "boolean",
                        "description": "Точное совпадение имени"
                    }
                },
                "required": ["role", "name"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_click_text",
            "description": (
                "Находит видимый элемент по его тексту и нажимает его. "
                "Используй этот инструмент в первую очередь, когда известен "
                "текст кнопки, ссылки, строки таблицы, вкладки или другого "
                "видимого элемента. Это предпочтительнее перебора element_id."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "text": {
                        "type": "string",
                        "description": "Видимый текст элемента"
                    },
                    "exact": {
                        "type": "boolean",
                        "description": (
                            "Точное совпадение текста. "
                            "По умолчанию true."
                        )
                    }
                },
                "required": ["text"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_click",
            "description": (
                "Нажимает видимый элемент страницы по element_id."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "element_id": {
                        "type": "string"
                    }
                },
                "required": ["element_id"]
            }
        }
    }
]


def execute_tool(name: str, arguments: dict):
    if name == "browser_open_page":
        return open_page(arguments["url"])

    if name == "browser_get_state":
        return get_state()

    if name == "browser_fill":
        return fill(
            arguments["element_id"],
            arguments["text"],
        )

    if name == "browser_click_role":
        return click_role(
            arguments["role"],
            arguments["name"],
            arguments.get("exact", True),
        )

    if name == "browser_click_text":
        return click_text(
            arguments["text"],
            arguments.get("exact", True),
        )

    if name == "browser_click":
        return click(
            arguments["element_id"]
        )

    return {
        "error": f"Unknown tool: {name}"
    }

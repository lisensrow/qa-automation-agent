from tools.browser import (
    open_page,
    get_state,
    fill,
    click_semantic,
)


TOOLS = [
    {
        "type": "function",
        "function": {
            "name": "browser_open_page",
            "description": (
                "Открывает URL в постоянной браузерной сессии Chromium. "
                "Возвращает фактическое состояние страницы и screenshot."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "url": {
                        "type": "string",
                        "description": "Полный URL страницы"
                    }
                },
                "required": ["url"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_get_state",
            "description": (
                "Получает текущее состояние уже открытой страницы. "
                "Используй только если актуального состояния "
                "от предыдущего browser tool недостаточно."
            ),
            "parameters": {
                "type": "object",
                "properties": {}
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_fill",
            "description": (
                "Вводит обычный несекретный текст в поле по element_id. "
                "Не использовать для паролей, токенов и секретов."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "element_id": {
                        "type": "string"
                    },
                    "text": {
                        "type": "string"
                    }
                },
                "required": [
                    "element_id",
                    "text"
                ]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "browser_click_semantic",
            "description": (
                "Нажимает видимый элемент по его смысловому имени. "
                "Сам определяет подходящую accessibility role "
                "(tab, button, link и другие), а если role нет — "
                "использует уникальный видимый текст. "
                "Не нужно угадывать role или element_id."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "name": {
                        "type": "string",
                        "description": (
                            "Точное видимое или доступное имя элемента"
                        )
                    },
                    "exact": {
                        "type": "boolean",
                        "description": (
                            "Точное совпадение. По умолчанию true."
                        )
                    }
                },
                "required": ["name"]
            }
        }
    }
]


def execute_tool(name: str, arguments: dict):
    if name == "browser_open_page":
        return open_page(arguments["url"])

    if name == "browser_get_state":
        return get_state()

    if name == "browser_fill":
        return fill(
            arguments["element_id"],
            arguments["text"],
        )

    if name == "browser_click_semantic":
        return click_semantic(
            arguments["name"],
            arguments.get("exact", True),
        )

    return {
        "error": f"Unknown tool: {name}"
    }
